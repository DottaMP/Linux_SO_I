# Fazer um programa que receba um número e imprima a contagem progressiva até chegar a 15.

#!/bin/sh
CONTADOR="$1"
while [ $CONTADOR -lt 16 ] 
do
    echo "$CONTADOR"
    sleep 0.5
    CONTADOR=$((CONTADOR+1))
done

exit 0
